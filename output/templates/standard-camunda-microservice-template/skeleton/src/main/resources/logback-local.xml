<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true">
    <springProperty scope="context" name="springAppName"
                    source="spring.application.name" scan="true" scanPeriod="30 seconds" debug="true"/>


    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="com.cma.core.common.config.logmask.MaskingPatternLayout">
                <maskPattern>uid [A-Za-z0-9]+</maskPattern>
                <maskPattern>(\w+@\w+\.\w+)</maskPattern>
                <pattern>[%d{dd-MM-yyyy HH:mm:ss.SSS}] [%-5level] [${SPRING_PROFILES_ACTIVE}] [${springAppName}]
                    [%X{traceId:-}] [%X{spanId:-}] [%thread] [%lo{10}:%M - %msg]%n
                </pattern>
            </layout>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>