apiVersion: v1
kind: Service
metadata:
  name: ${{values.project_name}}
  namespace: {{k8s.namespace}}
  labels:
    k8s-app: ${{values.project_name}}
spec:
  selector:
    k8s-app: ${{values.project_name}}
  type: ClusterIP
  ports:
  - name: web
    port: 8080
    protocol: TCP
 
