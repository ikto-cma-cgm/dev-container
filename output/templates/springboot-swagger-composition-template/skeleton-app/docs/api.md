# API Reference

The API contract is defined by the API entity `${{ values.apiRef }}` and fetched from its repository during the Maven build.

Swagger UI is available at `http://localhost:8080/swagger-ui.html` when running locally.
