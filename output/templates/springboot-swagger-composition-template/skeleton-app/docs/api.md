# API Reference

The API contract is defined in `src/main/resources/api/openapi.yaml` and is consumed from `${{ values.swaggerUrl }}` during the Maven build.

Swagger UI is available at `http://localhost:8080/swagger-ui.html` when running locally.
