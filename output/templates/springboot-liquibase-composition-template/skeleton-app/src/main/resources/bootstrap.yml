spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
      secrets:
        paths: "/etc/secrets"
  logging:
    config: classpath:logback.xml
management:
  endpoint:
    restart:
      enabled: true

---
spring:
  config:
    activate:
      on-profile:
        - local
  logging:
    config: classpath:logback.xml
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
